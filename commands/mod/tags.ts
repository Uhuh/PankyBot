import { Message } from "discord.js";
import PankyBot from "../../src/bot";

export default {
  desc: 'Add tags',
  name: 'tag',
  args: '<name_like_this> <description stuff>',
  type: 'mod',
  run: (_message: Message, _args: string[], _client: PankyBot) => {



    return;
  }
}
